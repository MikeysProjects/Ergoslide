if SHIELD_ERGOSLIDE_LEFT

# Name must be less than 16 characters long!
config ZMK_KEYBOARD_NAME
    default "Ergoslide"

config ZMK_SPLIT_ROLE_CENTRAL
    default y

endif

if SHIELD_ERGOSLIDE_LEFT || SHIELD_ERGOSLIDE_RIGHT

config ZMK_SPLIT
    default y

endif
